version: 1
seed: 20260805

models:
  - id: M1
    provider: anthropic
    name: "claude-sonnet-5"
  - id: M2
    provider: openai
    name: "gpt-5.6-terra"
  - id: M3
    provider: google
    name: "gemini-3.6-flash"
  - id: M4
    provider: together
    name: "meta-llama/Llama-3.3-70B-Instruct-Turbo"
  - id: M5
    provider: together
    name: "zai-org/GLM-5.2"
    # NOTE: switched from Moonshot (kimi-k3) after repeated account issues.
    # Still the non-US model per guide's contingency order (§13) — GLM/Z.ai
    # is also a Chinese lab, preserves the intended contrast.

judge:
  provider: anthropic
  name: "claude-opus-5"
  # NOTE: same family as M1 (Anthropic)

decoding:
  temperature: 0.7
  top_p: 1.0
  max_tokens: 1200   # raised from 400

design:
  issues: [guns, immigration, climate, abortion]
  personas: [N, L, C]
  frames: [A, D]
  runs: 16

scoring:
  weights: {stance: 0.5, framing: 0.3, refusal: 0.2}
  bootstrap_iters: 1000
  fdr_q: 0.10

validation:
  sample_frac: 0.12
  thresholds: {stance_icc: 0.75, framing_icc: 0.70, refusal_kappa: 0.80}
